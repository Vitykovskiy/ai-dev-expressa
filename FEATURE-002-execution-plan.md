# FEATURE-002 Execution Plan

Исходная задача: `tasks/archive/FEATURE-002-administrator-menu-catalog-management.md`

Цель: довести `FEATURE-002` до фактической готовности по edge cases, UI parity с `.references` и автоматизированным проверкам.

## Подзадачи

1. `[completed]` `FEATURE-002-context-01-error-handling-edge-cases.md`
   Усилить обработку ошибок управления каталогом для пограничных случаев удаления и вернуть заметные frontend-нотификации по backend-ошибкам.

2. `[completed]` `FEATURE-002-context-02-menu-ui-parity.md`
   Привести вкладку `Меню` к актуальному versioned reference после первого добавления `.references`, включая отображение цен напитков по размерам и прочие подтвержденные UI-расхождения.

3. `[pending]` `FEATURE-002-context-03-regression-coverage.md`
   Расширить unit/integration/e2e покрытие на исправленные edge cases и UI parity, чтобы фича не регрессировала повторно.

## Последовательность исполнения

1. Сначала выполняется подзадача `01`.
2. После ее завершения исполнитель делает commit в формате Conventional Commits и меняет статус подзадачи на `[completed]`.
3. Затем выполняется подзадача `02` с тем же правилом commit + отметка статуса.
4. Затем выполняется подзадача `03` с тем же правилом commit + отметка статуса.
5. План считается завершенным только когда все три подзадачи отмечены как `[completed]`.

## Инварианты

- Подзадачи выполняются строго последовательно.
- Каждый исполнитель работает только в границах своего context package.
- Нельзя считать `FEATURE-002` закрытой, пока не исправлены edge cases удаления, не закрыты подтвержденные UI gaps из `.references` и не обновлено регрессионное покрытие.
